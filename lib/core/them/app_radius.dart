import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

class AppRadius {
static BorderRadius sm   = BorderRadius.circular(10.sp);
static BorderRadius md   = BorderRadius.circular(16.sp);
static BorderRadius lg   = BorderRadius.circular(24.sp);
static BorderRadius xl   = BorderRadius.circular(32.sp);  // search bar
static BorderRadius card = BorderRadius.circular(12.sp);  // كروت المركبات
static BorderRadius full = BorderRadius.circular(100.sp); // badges مستديرة
}